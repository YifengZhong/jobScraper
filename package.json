{
  "name": "jobScrapper",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "SLS_DEBUG=* sls offline start -s local --log-level info --cacheInvalidationRegex 'node_modules'",
    "deploy": "sls deploy",
    "installDB": "sls dynamodb install",
    "localDB": "sls dynamodb start --migrate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.463.0",
    "puppeteer-autoscroll-down": "^0.1.1",
    "puppeteer-lambda": "^1.1.1",
    "serverless": "^1.43.0",
    "serverless-http": "^2.0.2",
    "serverless-offline": "^4.10.6"
  },
  "devDependencies": {
    "serverless-apigw-binary": "^0.4.4"
  }
}
